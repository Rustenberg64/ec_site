<div class="container">
  <div class="row g-5 justify-content-center">
    <%= image_tag @product.image, class: "img-fluid max-width: 100%; , h-100 col-10 col-md-6", size: '' %>

    <div class="col-10 col-md-5 mb-4">
      <h1><%= @product.manufacturer%> <%= @product.name%></h1>
      <h2>¥ <%= @product.price%></h2>
      <h4 class="mb-3">
        <% if @product.stock != 0%>
        <span class="fw-bold"><%="In Stock"%> </span>
        <% else %>
        <span class="fw-bold"><%="Out of Stock"%> </span>
        <% end %>
        (<%= @product.stock%>)
      </h4>

      <div class="d-grid mb-5">
        <button class="btn btn-primary" type="button">Button</button>
      </div>
      <p class="text-break"><%= @product.description %></p>
    </div>
  </div>
</div>