<div class="container">
<div class="row row-cols-2 row-cols-md-6 g-4">
  <% @products.each do |product|%>
    <div class="col">
      <div class="card h-100 border-0">
        <%= link_to image_tag( product.image, class: "img-fluid max-width: 100%; , height: auto;", size: '') , product_path(product)%>
        <div class="card-body text-center">
          <div class="card-text">
            <%= product.name %> <br>
            <%= product.manufacturer %> <br>
            ¥ <%= product.price %>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
</div>